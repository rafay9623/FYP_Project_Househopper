'use client' import * as React from 'react' import * as TabsPrimitive from '@radix-ui/react-tabs' import {cn} from '@/utils/helpers' function Tabs({className, ...props}) {return ()} function TabsList({className, ...props}) {return ()} function TabsTrigger({className, ...props}) {return ()} function TabsContent({className, ...props}) {return ()} export {Tabs, TabsList, TabsTrigger, TabsContent} 
